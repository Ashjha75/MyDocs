import mermaid from "mermaid";

mermaid.initialize({
  startOnLoad: true,
  theme: "dark", // 
  themeVariables: {
    primaryColor: "#1e88e5",
    primaryBorderColor: "#1565c0",
    primaryTextColor: "#fff"
  }
});
